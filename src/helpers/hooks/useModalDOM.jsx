import { useLayoutEffect } from "react";
import { addClass } from "helpers/format/classNameModifier.js";

const useModalDOM = () => {
  useLayoutEffect(() => {
    first;

    return () => {
      second;
    };
  }, [third]);
};

export default useModalDOM;
